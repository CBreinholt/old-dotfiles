#!/bin/bash

if [ $1 ]; then dest="$(echo $1 | sed 's/[^a-zA-z]*$/\//')"; else dest=$PWD; fi

dir="${dest}chris_backup"
echo "==>  Creating $dir"
mkdir -p $dir

echo "==>  Saving / to $dir"
rsync -aHAXS --info=progress2 /home/chris $dir
